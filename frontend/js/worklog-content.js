window.AI_WORKLOG_HTML = `
<h2>AI Worklog</h2>
<p>Честный журнал того, как был сделан этот проект: что делал человек, что — ИИ,
и что осталось бы сделать в реальном продукте.</p>

<h3>Использованные AI-инструменты</h3>
<ul>
  <li><strong>Claude (Cowork, модель claude-sonnet-5)</strong> — написал почти весь код бэкенда,
  фронтенда, миграций, тестов, README и этот Worklog по детальному техническому заданию,
  сформулированному разработчиком.</li>
  <li>Других AI-инструментов в этой итерации не использовалось.</li>
</ul>

<h3>Время и токены</h3>
<table>
  <thead><tr><th>Показатель</th><th>Значение</th></tr></thead>
  <tbody>
    <tr>
      <td>Общее время разработки</td>
      <td>Точный секундомер не вёлся. Проект собран за одну рабочую сессию в Cowork
      17 сентября 2026 года — от постановки ТЗ до готового кода. Честная оценка "по ощущениям":
      2–3 часа с учётом двух раундов уточняющих вопросов по стеку и деплою. Если нужна точная
      цифра — ориентируйтесь на метки времени первого и последнего сообщения в этом чате.</td>
    </tr>
    <tr>
      <td>Израсходованные токены</td>
      <td>Интерфейс Cowork не показывает разработчику точный расход токенов на конкретную задачу
      (только общий баланс аккаунта), поэтому честно: <strong>токены не считал(а) — инструмент не
      отдаёт эти данные на уровне задачи</strong>. Точную цифру можно посмотреть в биллинге/usage
      dashboard Anthropic, если он подключён.</td>
    </tr>
  </tbody>
</table>

<h3>Этапы работы</h3>


<h4>1. Архитектура</h4>
<p><strong>Разработчик:</strong> сформулировал ТЗ — бюро переселения привидений: заявки,
места переселения, автоматический подбор с объяснением, финальный отчёт; деплой пока не
нужен, пушить в GitHub будет сам.</p>
<p><strong>AI:</strong> спроектировал схему из трёх таблиц (<code>ghosts</code>,
<code>locations</code>, <code>assignments</code>) и сразу заложил разделение бэкенда на явные
слои — репозиторий (только доступ к данным), сервис (бизнес-логика и граница транзакции),
роутер (тонкий, просто вызывает сервис) — а сам алгоритм подбора вынес в чистый модуль без
обращения к сессии БД, чтобы его можно было тестировать напрямую, без базы.</p>

<h4>2. Выбор инструментов</h4>
<p><strong>Разработчик:</strong> утвердил стек — FastAPI + PostgreSQL + SQLAlchemy (async) +
Alembic + uv на бэкенде, обычные HTML/CSS/JS без фреймворка на фронтенде, всё в Docker
Compose (отдельные сервисы под миграции, сидинг демо-данных, backend и nginx), логирование —
через loguru.</p>
<p><strong>AI:</strong> реализовал выбранный стек: docker-compose.yml с сервисами
<code>db</code> → <code>migrate</code> → <code>seed</code> → <code>backend</code>,
<code>nginx</code> отдельным сервисом — раздаёт статику фронтенда и проксирует
<code>/api/</code> на backend; единый <code>.env.example</code> и <code>pyproject.toml</code>
в корне репозитория на весь проект, а не по одному на каждую часть; <code>loguru</code>
настроен на весь backend — консоль и файл <code>backend/logs/app.log</code>.</p>

<h4>3. Алгоритм подбора мест</h4>
<p><strong>AI:</strong> реализовал алгоритм в два шага — сначала жёсткие условия (вместимость,
дедлайн, явные конфликты вроде "боится зеркал" + место с зеркалами), затем взвешенный рейтинг
0–100 по температуре, шуму/тревожности, влажности и тесноте с текстовым объяснением на русском.
Для пакетного подбора заявки обрабатываются по срочности (ближайший дедлайн → выше
тревожность), чтобы место не "съедали" менее срочные заявки, а несколько заявок, претендующих
на одно и то же место, учитывались честно в одном проходе.</p>
<p><strong>Разработчик:</strong> формулировки особых условий и типов мест — из исходного ТЗ.</p>

<h4>4. REST API и слои бэкенда</h4>
<p><strong>AI:</strong> довёл разделение из этапа 1 до конца — <code>app/repositories/</code>
(только запросы к БД, без бизнес-правил и без commit), <code>app/services/</code>
(<code>GhostService</code>, <code>LocationService</code>, <code>MatchingService</code>,
<code>ReportService</code> — вся бизнес-логика и коммиты), тонкие роутеры, которые просто
вызывают сервис и переводят его исключения в HTTP-коды. Схемы Pydantic разложены по пакету
<code>app/schemas/</code> с файлом на каждую сущность, модели и enum'ы — в
<code>app/db/</code>, общие настройки — в <code>app/core/</code>.</p>

<h4>5. Фронтенд — вкладки Заявки / Места / Подбор / Отчёт</h4>
<p><strong>AI:</strong> сделал одностраничный интерфейс на чистом JS: формы добавления с
чекбоксами особых условий, карточки с понятными подписями вместо голых enum-значений,
<code>api.js</code> — обёртка над <code>fetch</code> с единообразной обработкой ошибок
сервера и сети, вкладка «AI Worklog» с этим текстом.</p>

<h4>6. Логика вкладки «Подбор»: кнопка «Обновить предложения» и ручное назначение</h4>
<p><strong>AI:</strong> кнопка «Обновить предложения» (<code>POST /api/match/run</code>)
пакетно назначает места всем ещё не распределённым заявкам и реально сохраняет назначения
в базу, не трогая уже существующие ручные; ручное назначение
(<code>POST /api/match/manual</code>) проходит ту же проверку жёстких и мягких условий: жёсткие
конфликты (переполненность, явное противоречие условию) блокируются всегда, мягкие проблемы
(например, неидеальная температура) можно подтвердить и всё равно назначить — именно так
было сформулировано в ТЗ.</p>

<h4>7. Баг: поле «assigned» терялось при сериализации — карточки никогда не показывали
настоящий статус назначения</h4>
<p><strong>Разработчик:</strong> попросил переработать теги на вкладке «Подбор» — чтобы
после «Обновить предложения» у автоматически назначенных заявок появлялся тег «автоматически
подобрано», у тех, кому место не нашлось — тег «нужен ручной подбор» с кнопкой «Назначить»
рядом, а после ручного назначения тег менялся на «подобрано ручным режимом»; также убрать
красный баннер-заглушку с датой сборки и надпись «— лучшее авто-предложение» у уже
сохранённых назначений.</p>
<p><strong>AI:</strong> при переработке нашёл настоящую причину, почему старые теги вообще
путали: схема <code>MatchResultOut</code> не объявляла поле <code>assigned</code>, хотя
<code>MatchingService</code> передавал его в конструктор — pydantic молча отбрасывал
незнакомое поле при сериализации ответа, и фронтенд никогда не получал
<code>assigned: true</code>, даже для по-настоящему сохранённого автоназначения. Из-за этого
карточка выглядела как несохранённое предложение всегда, независимо от реального состояния
в базе. Добавил <code>assigned: bool</code> в схему, переписал отрисовку карточек
(<code>renderMatchCard()</code>) на три чётких состояния — сохранено (авто или вручную),
ждёт запуска автоподбора, и «нужен ручной подбор» с кнопкой «Назначить» рядом — убрал
красный баннер-заглушку из <code>index.html</code> и отладочную панель с сырым JSON-ответом
сервера, которая была нужна только на время поиска этого самого бага.</p>

<h4>8. Тесты приведены в соответствие с текущей логикой</h4>
<p><strong>AI:</strong> дополнил <code>backend/tests/test_api.py</code> тестами, которые
напрямую проверяют то, что раньше молча ломалось: что <code>assigned</code> становится
<code>true</code> именно после сохранения (а не только в момент вызова), что предпросмотр
до нажатия «Обновить предложения» отдаёт <code>assigned: false</code>, и что повторный запуск
кнопки не трогает уже существующие ручные назначения. Юнит-тесты алгоритма
(<code>test_matching.py</code>) не менялись — баг был в сериализации API-ответа, а не в самом
алгоритме подбора.</p>
<p><strong>Честно:</strong> в песочнице этой сессии по-прежнему нет доступа к PyPI (сетевая
политика организации блокирует установку пакетов), поэтому <code>pytest</code> ни разу не
запускался внутри контейнера. Новые и старые тесты проверены синтаксически
(<code>py_compile</code>) и вручную прослежены построчно против кода алгоритма и сервиса —
обязательно прогоните <code>pytest</code> сами перед сдачей.</p>

<h4>9. Итог: функциональность реализована полностью и работает</h4>
<p><strong>Разработчик и AI:</strong> на этом раунде правок закрыт весь запрошенный
функционал вкладки «Подбор» — автоматический подбор по кнопке с сохранением в базу,
корректные теги «автоматически подобрано» / «нужен ручной подбор» / «подобрано ручным
режимом» с правильной логикой переключения между ними, кнопка «Назначить» именно там, где
она нужна, убран мусор из интерфейса (баннер сборки, отладочная панель, надписи про
несуществующее ещё предложение). Корень путаницы с тегами — отсутствующее поле
<code>assigned</code> в API-схеме — найден и устранён, а не просто замаскирован на
фронтенде, и это закреплено тестами, чтобы не повториться незаметно в будущем.</p>

<h3>Решения, принятые разработчиком самостоятельно</h3>
<ol>
  <li>Стек: FastAPI + PostgreSQL + Alembic + uv на бэкенде, обычный JS/CSS/HTML на фронтенде,
  упаковка в Docker Compose.</li>
  <li>Публичный деплой отложен — на этом этапе всё работает только локально.</li>
  <li>Push финального кода в GitHub-репозиторий разработчик делает сам, а не поручает AI.</li>
  <li>Состав особых условий привидений и типов мест переселения — задан в исходном ТЗ.</li>
  <li>Политика ручного выбора места: жёсткие конфликты (например, переполненность или явное
  нарушение условия) блокируются всегда без возможности "продавить"; мягкие проблемы (например,
  неидеальная температура) можно подтвердить и всё равно назначить — именно так было
  сформулировано в ТЗ ("если выбор плохой — предупредить, что не так").</li>
  <li>Архитектура бэкенда после первой версии переведена на явные три слоя —
  repository / service / endpoint — и модели оставлены только в виде SQLAlchemy-классов,
  без параллельного набора dataclass'ов.</li>
  <li>Файлы дальше разложены по core/db/schemas, а зависимости uv (pyproject.toml)
  перенесены из <code>backend/</code> в корень репозитория как общие для всего проекта.</li>
  <li><code>.env</code>/<code>.env.example</code> сделаны едиными на весь проект, вне
  <code>backend/</code> и <code>frontend/</code>.</li>
  <li>Команда запуска убрана из Dockerfile, миграции Alembic вынесены в отдельный
  сервис <code>migrate</code> в docker-compose.yml — так виден явный шаг, который
  их применяет, а не спрятанная в образе команда.</li>
  <li>Из кода убраны все построчные <code>#</code>/<code>//</code>-комментарии, но
  docstring'и осознанно оставлены как часть документации кода, а не "шум".</li>
  <li>Сидинг демо-данных и раздача фронтенда вынесены из backend'а в отдельные сервисы
  (<code>seed</code> и <code>nginx</code>) — backend занимается только API, а запуск
  всего проекта происходит исключительно через Docker Compose, без отдельного режима
  "без Docker" для самого приложения.</li>
  <li>Кнопка «Обновить предложения» реально назначает места (пакетно, с учётом
  конкуренции нескольких заявок за одно место) — с этим поведение прошло полный круг:
  сперва сохраняло автоматически, на этапе 18 стало только предлагать, на этапе 21
  снова стало сохранять, но теперь с явными тегами "назначено автоматически" /
  "автоматически не получилось" / "ручной выбор", чтобы не путать источники правды,
  как это было раньше. Оператор по-прежнему может в любой момент переназначить
  вручную.</li>
</ol>

<h3>Где AI мог ошибиться или предложить неидеальное решение</h3>
<ul>
  <li>Алгоритм подбора — <strong>жадный</strong> (greedy): заявки обрабатываются по одной в порядке
  срочности, а не оптимизируются глобально. Это значит, что в редких случаях общее число
  расселённых привидений может быть не максимально возможным (настоящая оптимизация потребовала бы
  венгерского алгоритма или похожей техники паросочетания).</li>
  <li>Пороги "мягкого" рейтинга (например, что рейтинг ниже 40/100 — это предупреждение) выбраны
  AI на глаз, без реальных данных бюро для калибровки — в реальном продукте их стоит проверить
  на исторических случаях.</li>
  <li>Как честно указано выше — интеграционные и юнит-тесты не были фактически запущены через
  pytest в этой сессии из-за сетевых ограничений песочницы; проверялась только логика алгоритма
  отдельным скриптом.</li>
  <li>Не проводилась проверка доступности (accessibility) фронтенда и кросс-браузерное тестирование.</li>
  <li>Рефакторинг на repository/service/endpoint (этап 7) тоже не прогонялся через pytest/uvicorn
  по той же причине — проверен только компиляцией и внимательным ручным чтением каждого метода.
  Логика подбора не менялась, но склейка слоёв (транзакции, порядок commit/flush) — новый код,
  и именно его в первую очередь стоит прогнать тестами перед сдачей.</li>
  <li>На этапе 12 (удаление комментариев) первая версия скрипта на <code>tokenize</code> содержала
  баг, который молча обрезал два файла миграций — конкретный пример того, как автоматическая
  правка кода без реального запуска тестов может незаметно сломать файл. Ошибка была замечена
  и исправлена в той же сессии (см. "Честно" в этапе 12), но это повод перепроверить именно
  Alembic-миграции особенно внимательно перед сдачей.</li>
</ul>

<h3>Что доработать вручную перед сдачей</h3>
<ul>
  <li>Реально запустить <code>docker compose up</code> и пройти чек-лист сценариев ниже.</li>
  <li>Выполнить <code>pytest</code> внутри контейнера/venv и убедиться, что все тесты зелёные.</li>
  <li>Проверить UI глазами: формы, вкладки, подсказки об ошибках.</li>
  <li>Решить вопрос публичного хостинга (Render/Railway/другое), если он нужен для итоговой сдачи.</li>
</ul>

<h3>Что улучшить, если бы это был реальный продукт</h3>
<ul>
  <li>Заменить жадный алгоритм на честную оптимизацию паросочетания (например, венгерский алгоритм),
  чтобы максимизировать число расселённых привидений, а не просто удовлетворить самых срочных.</li>
  <li>Аутентификация и роли операторов, журнал аудита (кто и когда менял назначение).</li>
  <li>Импорт заявок и мест из CSV/Excel, экспорт отчёта.</li>
  <li>Уведомления о приближающемся дедлайне переселения.</li>
  <li>CI/CD с автотестами на каждый PR и реальный деплой с managed Postgres.</li>
  <li>Более гибкая модель "особых условий" — конструктор правил вместо фиксированного enum.</li>
  <li>End-to-end тесты (например, Playwright) поверх текущих unit/integration тестов.</li>
  <li>Если фронтенд начнёт расти — перейти на React/Vue вместо чистого JS.</li>
  <li>Сейчас набор параметров привидения жёстко зашит в схему (фиксированный enum особых
  условий, числовые поля вроде температуры и тревожности) — в реальном продукте у духов
  параметры почти наверняка динамические: заявка от раза к разу может описывать что-то
  новое, чего вообще нет ни в enum'е, ни в колонках базы (например, "боится звука
  определённого колокола" или "может жить только рядом с конкретным деревом"). Жёсткая
  структура такое просто не примет или молча проигнорирует. Здесь есть смысл подключить
  LLM не вместо самого алгоритма подбора (жёсткие/мягкие условия и рейтинг 0–100 должны
  остаться детерминированными и объяснимыми, а не "чёрным ящиком"), а как отдельный слой
  перед ним: разбирать свободный текст заявки в структурированные условия там, где они
  укладываются в известную модель, и — что важнее — честно формулировать осмысленный
  ответ оператору там, где параметр действительно новый и его попросту нет ни в базе, ни
  в текущем наборе правил ("это условие раньше не встречалось, алгоритм не может его
  учесть — оцените этот случай вручную"), вместо того чтобы тихо пропустить его или
  свалиться с ошибкой валидации.</li>
</ul>
`;
