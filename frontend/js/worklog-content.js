window.AI_WORKLOG_HTML = `
<h2>AI Worklog</h2>
<p>Честный журнал того, как был сделан этот проект: что делал человек, что — ИИ,
и что осталось бы сделать в реальном продукте.</p>

<h3>Использованные AI-инструменты</h3>
<ul>
  <li><strong>Claude (Cowork, модель claude-sonnet-5)</strong> — написал почти весь код бэкенда,
  фронтенда, миграций, тестов, README и этот Worklog по детальному техническому заданию,
  сформулированному разработчиком.</li>
  <li>Других AI-инструментов в этой итерации не использовалось.</li>
</ul>

<h3>Время и токены</h3>
<table>
  <thead><tr><th>Показатель</th><th>Значение</th></tr></thead>
  <tbody>
    <tr>
      <td>Общее время разработки</td>
      <td>Точный секундомер не вёлся. Проект собран за одну рабочую сессию в Cowork
      17 сентября 2026 года — от постановки ТЗ до готового кода. Честная оценка "по ощущениям":
      2–3 часа с учётом двух раундов уточняющих вопросов по стеку и деплою. Если нужна точная
      цифра — ориентируйтесь на метки времени первого и последнего сообщения в этом чате.</td>
    </tr>
    <tr>
      <td>Израсходованные токены</td>
      <td>Интерфейс Cowork не показывает разработчику точный расход токенов на конкретную задачу
      (только общий баланс аккаунта), поэтому честно: <strong>токены не считал(а) — инструмент не
      отдаёт эти данные на уровне задачи</strong>. Точную цифру можно посмотреть в биллинге/usage
      dashboard Anthropic, если он подключён.</td>
    </tr>
  </tbody>
</table>

<h3>Этапы работы</h3>

<h4>1. Идея и уточнение ТЗ</h4>
<p><strong>Разработчик:</strong> прислал развёрнутое ТЗ на приложение бюро переселения привидений
со всеми обязательными сценариями и списком сдачи.</p>
<p><strong>AI:</strong> изучил подключённую папку проекта (пустой git-репозиторий с уже настроенным
origin на GitHub), затем задал два уточняющих вопроса вместо того, чтобы гадать: какой стек
использовать и нужен ли публичный деплой прямо сейчас.</p>
<p><em>Ключевые промпты:</em> исходное ТЗ целиком (см. первое сообщение в чате); затем два вопроса
через диалоговые кнопки о стеке и о деплое.</p>

<h4>2. Архитектура</h4>
<p><strong>Разработчик:</strong> выбрал стек — FastAPI + PostgreSQL + Alembic + uv на бэкенде,
обычный JavaScript/CSS/HTML на фронтенде, всё в Docker Compose; деплой пока не нужен, пушить
в GitHub будет сам.</p>
<p><strong>AI:</strong> спроектировал разделение на доменный слой (чистые dataclass'ы и алгоритм
подбора в <code>app/domain/</code>, без зависимости от FastAPI/SQLAlchemy — чтобы алгоритм можно
было тестировать без базы) и слой хранения/API поверх него; спроектировал схему из трёх таблиц
(ghosts, locations, assignments) и REST-эндпоинты.</p>

<h4>3. UI</h4>
<p><strong>AI:</strong> сделал одностраничный интерфейс на чистом JS с вкладками "Заявки",
"Места", "Подбор", "Отчёт" и "AI Worklog"; формы добавления с чекбоксами особых условий;
карточки с понятными подписями вместо голых enum-значений.</p>
<p><strong>Разработчик:</strong> на этом этапе визуально проект ещё не проверял — это стоит
сделать перед сдачей (см. чек-лист внизу README).</p>

<h4>4. Логика подбора</h4>
<p><strong>AI:</strong> реализовал алгоритм в два шага — сначала жёсткие условия (вместимость,
дедлайн, явные конфликты вроде "боится зеркал" + место с зеркалами), затем взвешенный рейтинг
0–100 по температуре, шуму/тревожности, влажности и тесноте с текстовым объяснением на русском.
Для пакетного подбора заявки обрабатываются по срочности (ближайший дедлайн → выше тревожность),
чтобы место не "съедали" менее срочные заявки.</p>
<p><strong>Разработчик:</strong> формулировки особых условий и типов мест — из исходного ТЗ.</p>

<h4>5. Отладка</h4>
<p><strong>AI:</strong> написал юнит-тесты алгоритма (без базы данных) и интеграционные тесты API
на SQLite. <strong>Важная честная оговорка:</strong> в песочнице этой сессии не было доступа к
PyPI/npm (сетевая политика организации блокирует установку пакетов — 403 host_not_allowed), поэтому
<code>pytest</code> в облачном контейнере запустить не удалось. Доменный алгоритм проверен вручную
отдельным Python-скриптом на тех же сценариях, что и в тестах, — все прошли. Но сами файлы
<code>tests/test_matching.py</code> и <code>tests/test_api.py</code> ни разу не выполнялись через
pytest внутри этой сессии — обязательно прогоните их сами перед сдачей (команда есть в README).</p>

<h4>6. Финальная проверка</h4>
<p><strong>Разработчик:</strong> должен запустить <code>docker compose up</code>, руками пройтись
по сценариям из чек-листа в README, прогнать <code>pytest</code>, и только после этого пушить
в свой GitHub-репозиторий и (по желанию) деплоить.</p>

<h4>7. Рефакторинг на repository/service/endpoint (после первой версии)</h4>
<p><strong>Разработчик:</strong> переложил <code>seed.py</code> в отдельную папку
<code>backend/seed_data/</code> и попросил переписать модели на SQLAlchemy без
параллельного набора dataclass'ов, а бэкенд — на явные три слоя: репозиторий,
сервис, эндпоинт.</p>
<p><strong>AI:</strong> убрал дублирующие dataclass-модели (<code>app/domain/models.py</code>) —
теперь единственный источник истины о данных — SQLAlchemy-классы
<code>Ghost</code>/<code>Location</code>/<code>Assignment</code> в <code>app/models.py</code>;
алгоритм подбора (<code>app/services/algorithm.py</code>) переписан на работу с ними напрямую
(он по-прежнему не трогает сессию БД, поэтому тестируется без базы). Добавлены
<code>app/repositories/</code> (только доступ к данным, без бизнес-правил и без commit),
<code>app/services/</code> (бизнес-логика + граница транзакции) и роутеры стали тонкими —
просто вызывают сервис и переводят его исключения в HTTP-коды. Импорт
демо-данных поправлен на новый путь (<code>seed_data.seed</code>) с защитой через
явное добавление корня backend в <code>sys.path</code>, чтобы не зависеть от того,
как именно запущен uvicorn.</p>
<p><strong>Честно про эту итерацию:</strong> в песочнице по-прежнему нет доступа к PyPI, поэтому
новый код (репозитории, сервисы, обновлённые роутеры) не запускался через pytest/uvicorn —
проверено через компиляцию (<code>py_compile</code>), grep на предмет забытых старых
импортов и внимательное ручное чтение каждого сервисного метода. Логика самого алгоритма
подбора не менялась и уже была проверена вручную в прошлой итерации. Обязательно прогоните
<code>pytest</code> и руками пройдите чек-лист после этого изменения — это первый реальный
прогон нового слоя репозиториев/сервисов.</p>

<h4>8. Разложение по core/db/schemas (после этапа 7)</h4>
<p><strong>Разработчик:</strong> справедливо заметил, что после первого рефакторинга
в папке <code>app/domain/</code> остался неиспользуемый файл <code>models.py</code>
(его не получилось удалить автоматически — см. ниже), и попросил довести структуру
до конца: разложить логически связанные файлы по отдельным директориям — схемы в
<code>schemas/</code>, модели БД и enum'ы в один пакет <code>db/</code>, общие
настройки проекта — в <code>core/</code>.</p>
<p><strong>AI:</strong> собрал <code>app/db/</code> (session.py с engine/сессией,
models.py, enums.py), <code>app/core/</code> (config.py) и разбил
<code>app/schemas.py</code> на пакет <code>app/schemas/</code> с файлом на каждую
сущность (ghost/location/matching/report) плюс <code>__init__.py</code>,
реэкспортирующий всё наружу — чтобы остальной код по-прежнему писал
<code>from app.schemas import GhostOut</code>, не зная о раскладке. Поправлены все
импорты в репозиториях, сервисах, роутерах, alembic/env.py и тестах.</p>
<p><strong>Честно:</strong> удалить старые файлы (<code>app/domain/</code>,
<code>app/repository.py</code>, а после этого шага ещё и устаревшие
<code>app/models.py</code>, <code>app/config.py</code>, <code>app/database.py</code>,
<code>app/enums.py</code>, <code>app/schemas.py</code> — их содержимое переехало в
<code>app/db/</code>, <code>app/core/</code> и <code>app/schemas/</code>) через
инструменты этой сессии так и не получилось — доступ к терминалу на компьютере
разработчика оставался недоступен ("workspace unavailable") на протяжении всей
работы. Новые файлы записаны поверх и работают независимо от старых, но старые
файлы физически остаются на диске, пока разработчик не удалит их сам. Это не
опционально: например, старый <code>app/schemas.py</code> лежит в той же папке,
что и новый пакет <code>app/schemas/</code> — их одновременное присутствие может
сбить с толку систему импорта Python. Перед первым запуском обязательно удалите
все перечисленные старые файлы и папки вручную.</p>

<h4>9. Общий pyproject.toml для uv (после этапа 8)</h4>
<p><strong>Разработчик:</strong> попросил перенести зависимости uv из
<code>backend/</code> в корень репозитория — общий файл на весь проект, а не
только на backend.</p>
<p><strong>AI:</strong> переместил <code>pyproject.toml</code> в корень (добавил
<code>testpaths = ["backend/tests"]</code> и поправил
<code>tool.hatch.build.targets.wheel.packages</code> на <code>["backend/app"]</code>,
так как путь к пакету теперь считается от корня, а не от <code>backend/</code>).
Docker-сборка переведена на корневой контекст (<code>docker-compose.yml</code>:
<code>build.context: .</code>, <code>dockerfile: backend/Dockerfile</code>), чтобы
образ мог увидеть оба файла и сохранить ту же структуру папок, что и в репозитории
(<code>backend/</code> остаётся подпапкой и внутри контейнера — это важно, иначе
относительный путь <code>backend/app</code> в <code>pyproject.toml</code> перестал
бы совпадать с реальным расположением файлов на диске). Команды в README поправлены:
установка зависимостей — из корня, запуск/миграции/тесты — как и раньше, из
<code>backend/</code> (или прямо из корня для <code>pytest</code>, у него теперь
явный <code>testpaths</code>).</p>
<p><strong>Честно:</strong> у меня по-прежнему нет доступа к терминалу на компьютере
разработчика, поэтому пересборка Docker-образа с новым <code>Dockerfile</code> ни
разу не проверялась по-настоящему — только вычитана построчно и сверена логика
путей. Обязательно прогоните <code>docker compose up --build</code> с нуля (можно
<code>docker compose build --no-cache backend</code>, чтобы точно не взять старый
слой) и убедитесь, что миграции и сидинг проходят.</p>

<h4>10. Один общий .env / .env.example (после этапа 9)</h4>
<p><strong>Разработчик:</strong> попросил, чтобы <code>.env</code> и <code>.env.example</code>
были в единственном экземпляре и лежали вне <code>backend/</code> и <code>frontend/</code> —
общие для всего проекта, а не по одному на каждую часть.</p>
<p><strong>AI:</strong> убрал отдельный <code>backend/.env.example</code>, объединил обе
переменные (<code>POSTGRES_*</code> для Docker и <code>DATABASE_URL</code> для локального
запуска без Docker) в один <code>.env.example</code> в корне. Чтобы backend находил этот
файл независимо от того, откуда его запускают (из <code>backend/</code>, из корня, из IDE),
<code>app/core/config.py</code> теперь резолвит путь к <code>.env</code> абсолютно —
через <code>Path(__file__).resolve()</code> с фиксированным числом уровней вверх до корня
репозитория, а не через относительный <code>env_file=".env"</code>, который зависел от
текущей рабочей директории процесса. Для Docker ничего не меняется: контейнеры получают
переменные напрямую через <code>environment:</code> в <code>docker-compose.yml</code>,
а не читают файл <code>.env</code> изнутри образа.</p>
<p><strong>Честно:</strong> как и в предыдущих шагах, реальный прогон (что backend
действительно находит корневой <code>.env</code> при разных способах запуска) не
выполнялся — только логика путей проверена вручную построчно.</p>

<h4>11. Миграции — отдельным сервисом в docker-compose (после этапа 10)</h4>
<p><strong>Разработчик:</strong> справедливо заметил, что раньше миграции Alembic
были спрятаны внутри команды запуска в Dockerfile — в docker-compose.yml не было
видно отдельного шага/контейнера, который их применяет. Попросил убрать команду
запуска из Dockerfile и перенести её в docker-compose, а миграции сделать
отдельным сервисом.</p>
<p><strong>AI:</strong> убрал <code>CMD</code> из <code>backend/Dockerfile</code> —
теперь у образа вообще нет команды по умолчанию, её задаёт каждый сервис в
docker-compose.yml через <code>command:</code>. Добавлен одноразовый сервис
<code>migrate</code> (тот же образ, команда — только <code>alembic upgrade head</code>,
без uvicorn), который поднимается после того, как <code>db</code> станет healthy,
и завершается. У <code>backend</code> добавлена зависимость
<code>migrate: condition: service_completed_successfully</code> — он стартует только
после того, как миграции успешно применились, а не параллельно с ними.</p>
<p><strong>Честно:</strong> реальный прогон <code>docker compose up --build</code> с
новой конфигурацией (сработает ли condition: service_completed_successfully, не
зависнет ли backend в ожидании) не выполнялся — терминал на компьютере разработчика
недоступен на протяжении всей этой сессии. Дополнительный риск здесь выше, чем в
предыдущих шагах: <code>service_completed_successfully</code> требует относительно
современную версию Docker Compose — если она старая, условие может не поддерживаться.
Проверьте версию (<code>docker compose version</code>) и сам факт, что <code>backend</code>
действительно ждёт <code>migrate</code>, а не падает из-за отсутствующих таблиц.</p>

<h4>12. Удаление комментариев из кода (после этапа 11)</h4>
<p><strong>Разработчик:</strong> попросил убрать все комментарии из кода. На уточняющий
вопрос (удалять ли вместе с ними и docstring'и — строки документации в тройных
кавычках) ответил: убрать только <code>#</code> и <code>//</code>, docstring'и оставить.</p>
<p><strong>AI:</strong> в 37 Python-файлах <code>backend/</code> удалены построчные
<code>#</code>-комментарии — не наивным regex/sed (он мог бы случайно задеть <code>#</code>
внутри строкового литерала), а через стандартный модуль <code>tokenize</code>: он
надёжно отличает токен <code>COMMENT</code> от токена <code>STRING</code> (к которому
относятся и docstring'и), поэтому docstring'и не тронуты. Строки, которые полностью
состояли из комментария, удалены целиком, а не оставлены пустыми. В трёх файлах фронтенда
(<code>api.js</code>, <code>app.js</code>, <code>worklog-content.js</code>) вручную убраны
однострочные <code>//</code>-комментарии (все они оказались отдельными строками, а не
частью URL или другой строки, так что править построчно было безопасно). Блочных
<code>/* */</code>-комментариев в JS и CSS не нашлось — <code>styles.css</code> не
изменился. Комментарии в <code>docker-compose.yml</code>, <code>backend/Dockerfile</code>
и <code>README.md</code> сознательно не тронуты — просьба звучала как "уберите из кода", а
эти файлы это конфигурация и документация, а не код приложения; уточнение по этой границе
не запрашивалось.</p>
<p><strong>Честно:</strong> при первой попытке написать скрипт на <code>tokenize</code> в
нём был баг — из-за <code>zip()</code> двух списков разной длины часть файла молча
обрезалась (пострадали <code>backend/alembic/env.py</code> и
<code>backend/alembic/versions/0001_initial.py</code>, которые скрипт успел перезаписать
до того, как ошибка всплыла на следующем файле). Это заметил и исправил в той же сессии:
переписал скрипт так, чтобы он сначала компилировал результат (<code>compile()</code>) и
только потом сохранял файл — и по тем же причинам добавил проверку компиляции ко всем
файлам перед записью. Файл <code>0001_initial.py</code> восстановлен по структуре таблиц
из <code>backend/app/db/models.py</code> (он не пострадал, так как обрабатывался позже
по алфавиту), <code>env.py</code> — по стандартной структуре Alembic <code>env.py</code>
для этого проекта. После исправления все 37 Python-файлов и 3 JS-файла проверены
(<code>py_compile</code>/<code>ast.parse</code> для Python, <code>node --check</code> для
JS) — компилируются, и точечно сверено наличие docstring'ов в нескольких файлах, где они
были. Реальный запуск приложения после этого изменения, как и раньше, не выполнялся.</p>

<h4>13. Сидинг — отдельным сервисом, фронтенд — отдельным nginx (после этапа 12)</h4>
<p><strong>Разработчик:</strong> попросил убрать из <code>main.py</code> заполнение базы
демо-данными в <code>lifespan</code> — вместо этого должен быть отдельный файл-скрипт,
который запускается в docker-compose и сам проверяет, есть ли уже данные в базе. Также
попросил убрать из <code>main.py</code> настройки, связанные с раздачей фронтенда, и
сделал явную оговорку: запуск проекта — исключительно через Docker.</p>
<p><strong>AI:</strong> из <code>app/main.py</code> убраны: сам <code>lifespan</code>
(и заодно ставший ненужным <code>sys.path</code>-хак для импорта <code>seed_data</code>),
монтирование статики фронтенда (<code>StaticFiles</code>, <code>FRONTEND_DIR</code>) и всё,
что с этим связано. Логика сидинга (<code>seed_if_empty</code>) не изменилась, но получила
отдельную точку входа — <code>backend/seed_data/run_seed.py</code> — которую в
docker-compose.yml запускает новый одноразовый сервис <code>seed</code> (по образцу уже
существовавшего <code>migrate</code>): поднимается после успешного завершения
<code>migrate</code>, сам решает, нужно ли что-то заполнять (проверка "пуста ли таблица
locations" осталась в <code>seed_if_empty</code>, как и раньше), и завершается. Сервис
<code>backend</code> теперь ждёт успешного завершения обоих (<code>migrate</code> и
<code>seed</code>), а не просто здоровья базы. Раздачу фронтенда, раз её больше нет в
backend'е, взял на себя новый сервис <code>nginx</code> (образ <code>nginx:alpine</code>,
конфиг — <code>nginx/nginx.conf</code>): раздаёт статику из <code>frontend/</code> и
проксирует <code>/api/</code> на <code>backend:8000</code>, поэтому относительные пути вида
<code>fetch("/api/ghosts")</code> во фронтенде менять не пришлось. Из настроек
(<code>app/core/config.py</code>) убрано поле <code>frontend_dir</code> — оно больше нигде
не используется; <code>seed_demo_data</code> оставлено, его теперь читает
<code>run_seed.py</code>, а не <code>main.py</code>. README переписан под новую схему запуска
(порт 8080 у nginx вместо 8000 у бэкенда для самого приложения, раздел "Локальный запуск без
Docker" убран — про тесты локально написано отдельно, они не то же самое, что запуск
приложения).</p>
<p><strong>Честно:</strong> как и всё в этом проекте, новая схема (два новых сервиса плюс
их зависимости в docker-compose, реверс-прокси в nginx.conf) не запускалась по-настоящему —
терминал на компьютере разработчика недоступен на протяжении всей сессии. Особое внимание
при первом реальном запуске стоит обратить на nginx.conf (правильно ли резолвится имя
сервиса <code>backend</code> внутри docker-сети, не понадобится ли <code>depends_on</code> с
условием вместо простого списка для nginx) и на то, что сервис <code>seed</code>, как и
<code>migrate</code>, не перезапускается автоматически при последующих <code>docker compose
up</code> без <code>--build</code>/пересоздания — если база уже не пуста, это ожидаемо
(он просто ничего не делает), но стоит проверить оба варианта из чек-листа ниже.</p>

<h4>14. Явное различие "назначено" / "предложено" на вкладке Подбор (после этапа 13)</h4>
<p><strong>Разработчик:</strong> заметил, что на вкладке «Подбор» непонятно, какие карточки —
это уже сохранённое в базе назначение, а какие — просто лучший вариант, который алгоритм
предложил на лету (эндпоинт <code>GET /api/match</code> для ещё не распределённых заявок
всегда считал такую подсказку заново, но в ответе API это никак не было помечено). Попросил
сделать это заметнее.</p>
<p><strong>AI:</strong> в схему <code>MatchResultOut</code> добавлено поле
<code>assigned: bool</code> — <code>true</code>, когда назначение реально сохранено в
таблице <code>assignments</code>, и <code>false</code>, когда это просто результат
<code>algorithm.match_ghost</code>, посчитанный "на лету" для ответа и никуда не записанный.
<code>MatchingService.current_status()</code> и <code>run_matching()</code> теперь явно
проставляют этот флаг для каждого случая. На фронтенде карточка с <code>assigned: true</code>
получила зелёную плашку "✓ Назначено" и сплошную рамку слева, а с <code>assigned: false</code> —
жёлтую плашку "◌ Предложено — пока не сохранено", пунктирную рамку и подсказку под текстом
про то, что нужно нажать «Запустить подбор», чтобы закрепить вариант. Заодно у ещё не сохранённых
карточек убрана кнопка «Снять назначение» — раньше она была видна у всех «matched»-карточек,
хотя реально снимать было нечего, если запись ещё не в базе.</p>
<p><strong>Честно:</strong> изменения в бэкенде затрагивают только чтение и формирование
ответа (сама логика подбора и коммитов не менялась), но, как и всё в этой сессии, реального
похода в браузер и клика по карточкам не было — визуально не проверено, как это выглядит
вживую. Стоит перед сдачей открыть вкладку «Подбор» и убедиться, что обе плашки действительно
хорошо различимы и текст подсказки не наезжает на остальную карточку на маленьких экранах.</p>

<h4>15. Быстрая кнопка «Назначить» на предложенном варианте (после этапа 14)</h4>
<p><strong>Разработчик:</strong> попросил на карточке с лучшим авто-предложением добавить
рядом с названием места подпись "— лучшее авто-предложение" и кнопку "Назначить" прямо там,
а не только через выпадающий список ниже.</p>
<p><strong>AI:</strong> в заголовок карточки (там же, где "Имя привидения → Название места")
добавлена курсивная подпись "— лучшее авто-предложение" для ещё не сохранённых карточек,
и рядом с заголовком — кнопка "Назначить", которая вызывает тот же <code>POST
/api/match/manual</code>, что и "Выбрать вручную" ниже, но сразу с
<code>location_id</code> предложенного места — не нужно ничего выбирать в списке. Кнопка
проходит через ту же проверку конфликтов на бэкенде (жёсткие блокируются, мягкие требуют
подтверждения "Всё равно назначить"), это просто более короткий путь к тому же результату.
Старый способ (выпадающий список + "Выбрать вручную") никуда не делся — он нужен, если
оператор хочет назначить <em>другое</em> место, а не предложенное.</p>
<p><strong>Честно:</strong> изменение чисто фронтендовое, бэкенд не трогали. Как обычно,
не проверено вживую — стоит перед сдачей убедиться, что кнопка "Назначить" не съезжает
на мобильной ширине рядом с длинными названиями мест.</p>

<h4>16. Правки после первого реального скриншота от разработчика (после этапа 15)</h4>
<p><strong>Разработчик:</strong> первый раз прислал скриншот реального интерфейса вместо
описания на словах — и на нём сразу нашлось два расхождения с тем, что задумывалось: текст
"— лучшее авто-предложение" и кнопка "Назначить" из этапа 15 на экране просто не появились,
а подсказка про несохранённый вариант (этап 14) визуально слиплась с последним пунктом списка
объяснений, без отступа.</p>
<p><strong>AI:</strong> вторую проблему нашёл и исправил сам — подсказка использовала общий
класс <code>.hint</code>, а у него <code>margin-top: -8px</code> (он был рассчитан на другое
место в интерфейсе, сразу под заголовком панели, а не после списка). Завёл отдельный класс
<code>.suggestion-hint</code> с нормальным отступом сверху. Первую проблему (текст и кнопка
из этапа 15 не видны) правкой кода объяснить не удалось — синтаксис в <code>app.js</code>
и раньше, и сейчас корректен (проверено <code>node --check</code>), а на скриншоте видны
как раз признаки предыдущей версии файла (плашка "Предложено — пока не сохранено" из этапа 14
есть, а кнопки из этапа 15 — ещё нет). Наиболее вероятная причина — браузер закэшировал старый
<code>app.js</code>: <code>nginx.conf</code> до этого не задавал никаких заголовков кэширования
для статики, а браузеры по умолчанию агрессивно кэшируют <code>.js</code>/<code>.css</code>.
Добавил в <code>nginx.conf</code> <code>Cache-Control: no-cache</code> для отдаваемой
статики — это не отключает кэш полностью, но заставляет браузer каждый раз перепроверять
файл на сервере (через <code>If-None-Match</code>/<code>ETag</code>), а не показывать
сохранённую копию вслепую.</p>
<p><strong>Честно:</strong> это предположение, не подтверждённый факт — я не вижу, что
реально произошло в браузере разработчика, поэтому не могу быть уверен, что дело именно
в кэше, а не в чём-то ещё (например, что скриншот сделан до перезапуска <code>docker
compose</code>, или что открыта другая вкладка со старой версией страницы). Если после
пересборки и жёсткого рефреша (<code>Ctrl+Shift+R</code>) текст и кнопка всё ещё не
появляются — это отдельная проблема, и её надо разбирать по факту (например, глянуть
вкладку Network в инструментах разработчика браузера и проверить, какой именно файл
<code>app.js</code> реально загрузился).</p>

<h4>17. Отступ так и не появился — переход с margin на flex-gap + версия в URL статики (после этапа 16)</h4>
<p><strong>Разработчик:</strong> прислал второй скриншот — кнопка и текст "лучшее
авто-предложение" из этапа 15 уже появились (значит, JS всё-таки обновился), но отступ
перед подсказкой "Это лучший вариант из доступных..." (этап 16) так и не появился, даже
после перезапуска контейнеров.</p>
<p><strong>AI:</strong> раз перезапуск не помог, а CSS-правило в файле было правильным
(<code>margin: 10px 0 0</code> у <code>.suggestion-hint</code>), решил не гадать дальше
о причине конкретно этого расхождения, а убрать саму механику, на которую полагался —
margin между соседними блочными элементами (<code>&lt;ul&gt;</code> и
<code>&lt;p&gt;</code>) может схлопываться (margin collapsing) в зависимости от контекста,
и полагаться на него было неудачным решением с самого начала. Переделал: контейнер
<code>.match-card .main</code> стал flex-контейнером с <code>flex-direction: column</code>
и <code>gap: 10px</code> — при таком подходе отступ между всеми прямыми потомками
(заголовок, тэги, список объяснений, подсказка, предупреждения) задаётся один раз в одном
месте и не может "схлопнуться", потому что <code>gap</code> у flex/grid контейнеров в
принципе не подвержен margin collapsing. У самих потомков (<code>.tags-row</code>,
<code>.suggestion-hint</code>, <code>.explanation</code>, <code>.reasons</code>) убраны
теперь уже лишние margin — их отступы задаёт только <code>gap</code>. Заодно, чтобы
исключить кэш как причину задержки навсегда, а не гадать про него в следующий раз —
добавил в <code>index.html</code> версию в конце пути статики
(<code>styles.css?v=2</code>, <code>app.js?v=2</code> и т.д.): раз в URL другая строка,
браузер обязан скачать файл заново, независимо от старых заголовков кэша. При следующих
правках фронтенда номер версии нужно будет увеличивать вручную.</p>
<p><strong>Честно:</strong> как и раньше, реального рендеринга в браузере не видел — не
могу быть на сто процентов уверен, что дело было именно в margin collapsing, а не в
чём-то ещё (например, в том, что менялся не тот файл, который реально раздаёт nginx, если
том Docker на Windows не всегда мгновенно подхватывает изменения с хоста). Переход на
flex-gap и версии в URL — это одновременно и вероятное исправление, и подстраховка на
случай, если причина была не в CSS, а в кэшировании файла где-то между хостом и браузером.</p>

<h4>18. Отступ так и не победили — пересмотр самой логики автоподбора (после этапа 17)</h4>
<p><strong>Разработчик:</strong> третий раз подряд отступ не появился, даже с отключённым
кэшем браузера. Вместо продолжения разбирательства с CSS попросил убрать саму подсказку
(строку про "нажмите «Запустить подбор»...") и переосмыслить логику: чтобы автоподбор
только показывал лучшее предложение, а место всегда назначал вручную оператор.</p>
<p><strong>AI:</strong> убрал подсказку целиком (вместе с уже неиспользуемым CSS-классом
<code>.suggestion-hint</code>) — это заодно закрывает вопрос с отступом, который так и не
разобрали до конца. Но главное — переделал логику на бэкенде, а не только текст на
фронтенде. Раньше было два разных поведения под одной кнопкой: <code>GET /api/match</code>
(открытие вкладки) просто показывал предложения, а <code>POST /api/match/run</code>
(кнопка «Запустить подбор») — по-настоящему сохранял назначения в базу для всех ещё не
распределённых вручную заявок. Это и было корнем всей путаницы с "назначено"/"предложено"
из этапов 14–17: два разных источника правды под одним экраном. Теперь оба эндпоинта
делают ровно одно и то же — <code>run_matching()</code> стал тонкой обёрткой над
<code>current_status()</code>, ничего не сохраняет и не коммитит. Раньше <code>MatchingService</code>
считал предложения для ещё не распределённых заявок по одной штуке отдельно
(<code>algorithm.match_ghost</code>), что могло привести к тому, что два привидения
одновременно "предлагались" бы в одно и то же место с одним свободным местом — не
пересчитано; заменил на пакетный <code>algorithm.match_all</code> (он и раньше был в
проекте, но использовался только при сохранении) — он честно учитывает срочность и общую
занимаемую вместимость между несколькими предложениями сразу, просто ничего не пишет в
базу (использует транзиентные копии мест, как и раньше). Единственный способ реально
назначить место остался прежним — <code>POST /api/match/manual</code> (кнопки «Назначить»,
«Выбрать вручную», «Назначить вручную» на карточке невозможного случая), и он как раньше
проходит проверку жёстких/мягких конфликтов. Кнопку на фронтенде переименовал в «Обновить
предложения» и поправил подсказку под вкладкой — текст больше не обещает, что кнопка
что-то "запускает и сохраняет".</p>
<p><strong>Честно:</strong> это меняет поведение отчёта (<code>ReportService</code>) —
теперь "расселено" в финальном отчёте считает только реально подтверждённые оператором
назначения, а не всё, что алгоритм когда-либо предложил и автоматически сохранил. Кажется,
что это и есть правильное поведение (отчёт был и раньше устроен именно так — читает только
таблицу <code>assignments</code> — так что фактически ничего в отчёте не сломалось, просто
теперь его цифры действительно отражают ручную работу оператора, а не автоматику). Из
тестов ни один не проверял именно факт сохранения через <code>run_matching</code> в базу
напрямую, поэтому <code>test_api.py</code> менять не пришлось — но раз реального прогона
<code>pytest</code> в этой сессии по-прежнему не было (доступа к терминалу нет), стоит
обязательно прогнать тесты перед сдачей и отдельно проверить сценарий "два привидения
претендуют на одно последнее место" глазами.</p>

<h4>19. Убран тег «ручной выбор»/«авто» — он больше ничего не добавляет (после этапа 18)</h4>
<p><strong>Разработчик:</strong> заметил, что рядом с зелёным тегом "✓ Назначено" (или
жёлтым "◌ Предложено") на карточке всё ещё висел второй тег — "ручной выбор" или "авто" —
и он не помогает понять, назначено ли место, а скорее путает.</p>
<p><strong>AI:</strong> так и есть, и это прямое следствие этапа 18: раз единственный
способ что-то сохранить в базу — это <code>POST /api/match/manual</code>, который всегда
проставляет <code>manual=True</code>, а всё ещё не сохранённое всегда считается с
<code>manual=False</code> (жёстко задано в <code>current_status()</code> для предложений) —
тег "ручной выбор"/"авто" стал на 100% дублировать то же самое, что уже показывает тег
"Назначено"/"Предложено", просто другими словами. До этапа 18 у него был смысл — тогда
существовали по-настоящему различные случаи: место могло быть сохранено автоматом (кнопка
«Запустить подбор») или оператором вручную, и это стоило показывать отдельно. Теперь такого
различия в данных больше нет, так что убрал тег целиком из карточек подбора — остался
только один статус на карточку. Поле <code>manual</code> в самой схеме
(<code>AssignmentOut</code>) не убирал — оно не мешает и может пригодиться, если в будущем
вернётся какой-то автоматический сценарий, просто на фронтенде оно больше нигде не
отображается.</p>
<p><strong>Честно:</strong> опять же не проверено вживую — стоит убедиться, что без второго
тега строка с тегами не выглядит "пустой" или странно смещённой.</p>

<h4>20. Тег "Предложено" не менялся на "Назначено" после назначения (после этапа 19)</h4>
<p><strong>Разработчик:</strong> после клика "Назначить" карточка оставалась с жёлтым тегом
"◌ Предложено — пока не сохранено", хотя назначение должно было пройти.</p>
<p><strong>AI:</strong> логику назначения и перерисовки на этот раз проверил построчно
и она в порядке — <code>tryManualAssign()</code> после успешного ответа
(<code>status === "assigned"</code>) вызывает <code>loadMatchStatus()</code>, которая
заново запрашивает <code>GET /api/match</code> и перерисовывает список; на бэкенде
<code>current_status()</code> строит список заново из базы при каждом запросе. Прямого
бага в этой цепочке не нашёл — но нашёл вероятную причину в другом месте: запрос
<code>GET /api/match</code> идёт через <code>fetch()</code> без явных указаний про
кэш, а браузер по умолчанию может использовать HTTP-кэш и для обычных
<code>fetch</code>-запросов, если сервер explicitно не запретил это заголовками — а
ответы <code>/api/...</code> до сих пор такого заголовка не имели (только статика
<code>frontend/</code> получила <code>Cache-Control</code> на этапах 16–17). То есть
после назначения браузер вполне мог показать не новый ответ сервера, а старый
закэшированный список статусов. Исправил в двух местах сразу, чтобы не зависеть от
поведения конкретного браузера: в <code>nginx.conf</code> добавил
<code>Cache-Control: no-store</code> для всего <code>/api/</code> (запрещает
кэширование на уровне HTTP-заголовков), и в <code>api.js</code> — <code>cache: "no-store"</code>
прямо в опциях <code>fetch()</code> (запрещает браузеру даже пытаться заглянуть в кэш,
независимо от заголовков ответа).</p>
<p><strong>Честно:</strong> как и с CSS-кэшем раньше, это тоже предположение по
наиболее вероятной причине, а не подтверждённый факт — сам поведение браузера не
наблюдал. Если после пересборки и обновления страницы (версия статики уже
<code>?v=5</code>) тег всё ещё не меняется на "Назначено" — значит дело не в кэшировании
API-ответов, и тогда стоит прямо во вкладке Network посмотреть, что реально возвращает
<code>GET /api/match</code> после клика "Назначить": там будет видно, приходит ли новое
значение <code>assigned: true</code> с сервера или нет, и это разделит "проблема на
бэкенде" и "проблема в отображении".</p>

<h4>21. Разворот этапа 18 — кнопка снова реально назначает места, плюс три тега (после этапа 20)</h4>
<p><strong>Разработчик:</strong> попросил переработать логику так, чтобы кнопка «Обновить
предложения» снова назначала лучший вариант по-настоящему (то есть сохраняла в базу), с
тегом, что назначено автоматически; тем заявкам, кому автоматом место не нашлось — отдельный
тег, что автоматически не получилось; а при ручном назначении — ещё один тег, что это ручной
выбор.</p>
<p><strong>AI:</strong> это прямой разворот решения из этапа 18 — там кнопку намеренно
сделали "только показывает, никогда не сохраняет", а теперь разработчик явно попросил
обратное. Никаких возражений не делал — просто реализовал так, как попросили.
<code>run_matching()</code> в <code>matching_service.py</code> снова стал "боевым": сначала
удаляет из базы все старые автоматические назначения (<code>manual=False</code>), которые
могли остаться с прошлого запуска (возвращая занятые места в вместимость через
<code>occupied -= 1</code>), затем пакетно (<code>algorithm.match_all</code> — та самая
конкурентно-честная версия с этапа 18, она никуда не делась) считает лучшие варианты для
всех ещё не занятых вручную заявок и сразу создаёт для них новые <code>Assignment</code>
с <code>manual=False</code> и коммитит. Уже существующие ручные назначения
(<code>manual=True</code>) кнопка не трогает и не пересчитывает — только автоматические.
На фронтенде (<code>renderMatching()</code> в <code>app.js</code>) теперь для карточек с
<code>assigned: true</code> показывается второй тег в зависимости от поля
<code>assignment.manual</code>: жёлтый "✋ Ручной выбор" или зелёный "⚙ Назначено
автоматически" — раньше (этап 19) этот тег убрали, потому что он полностью дублировал
тег "Назначено"/"Предложено"; теперь, когда назначенные карточки снова бывают двух разных
видов (автомат и вручную), различие в данных вернулось, и тег вернули вместе с ним, но
только рядом с "Назначено" — у "Предложено" второго тега по-прежнему нет, там нечего
различать. Для карточек с невозможным переселением (<code>status: "impossible"</code>)
добавлен отдельный тег "✕ Автоматически не получилось", чтобы было явно видно: это не
"ещё не считали", а "алгоритм проверил и не смог". Подсказку под кнопкой на вкладке
"Подбор" и раздел README "Как работает алгоритм подбора" переписал под новое поведение —
там раньше было прямо написано, что алгоритм "никогда сам не записывает результат в
базу", это стало неверным и требовало правки.</p>
<p><strong>Честно:</strong> как и почти всё в этом проекте, это не проверено реальным
запуском — доступа к терминалу/Docker в этой сессии по-прежнему нет, изменения прошли
только <code>py_compile</code> (бэкенд) и <code>node --check</code> (фронтенд), то есть
проверка на синтаксис, а не на поведение. Особенно стоит вручную перепроверить: (1) что
после повторного нажатия «Обновить предложения» старые авто-назначения действительно
корректно удаляются и вместимость мест (<code>occupied</code>) не "утекает" и не
задваивается при пересчёте; (2) что ручные назначения точно не трогаются повторным
нажатием кнопки; (3) визуально — как выглядит карточка с двумя тегами подряд ("Назначено"
+ "Ручной выбор" или "Назначено" + "Назначено автоматически"), не тесно ли и не наезжают
ли теги друг на друга.</p>

<h4>22. «Всё равно не сохраняет» — добавлено логирование через loguru на весь проект (после этапа 21)</h4>
<p><strong>Разработчик:</strong> после этапа 21 сообщил, что кнопка «Обновить предложения»
по-прежнему ничего не сохраняет и не обновляет, несмотря на то, что код был исправлен.
Попросил добавить логирование через loguru на весь проект.</p>
<p><strong>AI:</strong> перечитал логику <code>run_matching()</code> и связанного кода ещё
раз построчно и не нашёл в ней логической ошибки — она должна реально создавать записи
<code>Assignment</code> и коммитить их. Проблема в том, что у меня в этой сессии по-прежнему
нет возможности запустить backend по-настоящему (ни Docker, ни доступа к сети для установки
зависимостей вроде sqlalchemy в песочнице, чтобы прогнать логику отдельно) — то есть я не могу
ни подтвердить, ни опровергнуть баг сам, а просто повторное чтение кода уже не даёт новой
информации. Самая вероятная причина, которую называл и раньше — backend собирается в
Docker-образ (<code>build:</code> в docker-compose.yml), а не подключается как volume, поэтому
правки в <code>.py</code>-файлах не подхватываются без <code>docker compose up --build</code>.
Но чтобы не гадать дальше, сделал то, что попросили: настроил <code>loguru</code>
(<code>app/core/logging.py</code>) на весь backend — вывод одновременно в консоль
(<code>docker compose logs backend</code>) и в файл <code>backend/logs/app.log</code>
(проброшен в docker-compose.yml как volume, можно открыть прямо на компьютере, не заходя в
контейнер). Добавил: middleware в <code>main.py</code>, логирующий каждый входящий запрос и
код ответа (сразу видно, доходит ли <code>POST /api/match/run</code> до сервера вообще);
подробные логи внутри <code>run_matching()</code> — сколько заявок/мест/уже существующих
назначений на входе, сколько старых авто-назначений удалено, что получилось в каждом исходе
подбора, явный <code>try/except</code> вокруг <code>commit()</code> с логированием исключения
(если сохранение падает — теперь это будет видно в логе, а не тихо проглочено); то же самое
для <code>manual_assign()</code> и <code>unassign()</code>; более лёгкие логи создания/удаления
в <code>ghost_service.py</code>, <code>location_service.py</code> и
<code>report_service.py</code> — чтобы логирование было действительно на весь проект, а не
только в одном файле. Стандартные логи uvicorn и SQLAlchemy тоже перехватываются и идут в тот
же формат и тот же файл. Добавил зависимость <code>loguru</code> в <code>pyproject.toml</code>
и переменную <code>LOG_LEVEL</code> в <code>.env.example</code> (по умолчанию INFO, можно
поставить DEBUG для построчного разбора подбора).</p>
<p><strong>Честно:</strong> это не чинит саму проблему "не сохраняет" — если баг реальный
(а не в пересборке образа), логи только помогут его найти, а не исправляют его сами по себе.
После <code>docker compose up --build</code> и повторного нажатия кнопки нужно посмотреть в
<code>backend/logs/app.log</code>: если там есть строка "run_matching: создано новых
авто-назначений=N — коммит выполнен" с N больше нуля, а на экране всё равно "Предложено" —
значит проблема на фронтенде (не перерисовывается, кэш и т.д.). Если строки "run_matching:
старт" вообще нет в логе после нажатия кнопки — запрос не доходит до backend (фронтенд,
nginx, или всё ещё работает старый образ без middleware логирования запросов). Если
"commit() упал с исключением" — будет виден полный traceback, и это уже прямая причина. Как
и всё остальное здесь, сама настройка loguru не была запущена ни разу — синтаксис проверен
<code>py_compile</code>, но не поведение в реальном FastAPI-приложении.</p>

<h4>23. Значения manual в БД правильные, но на экране это не видно — защитная переработка renderMatching() (после этапа 22)</h4>
<p><strong>Разработчик:</strong> проверил поле <code>manual</code> напрямую в базе данных —
там реально есть и <code>true</code>, и <code>false</code> вперемешку, то есть backend
считает и сохраняет всё правильно. Но на вкладке «Подбор» это никак не отражается —
карточки не показывают разницу между авто- и ручными назначениями.</p>
<p><strong>AI:</strong> раз в базе данные правильные, а на экране нет — значит проблема
именно в отрисовке на фронтенде, и раньше в этом месте был реальный риск: функция
<code>renderMatching()</code> строила всю разметку одним большим <code>.map()</code> и
присваивала результат в <code>list.innerHTML</code> только один раз, всё сразу. Если бы
хоть у одной карточки в процессе сборки её разметки произошла ошибка (например, обращение
к полю объекта, которого не оказалось), <code>.map()</code> прерывался с исключением ДО
присваивания <code>innerHTML</code> — и на экране просто оставалось то, что было отрисовано
раньше, без единого сообщения об ошибке. Внешне это неотличимо от "кнопка не работает",
хотя сервер мог всё сохранить правильно. Переработал по трём направлениям: (1) разбил
<code>renderMatching()</code> на отдельную защищённую функцию <code>renderMatchCard()</code>
для одной карточки, которая нигде не берёт поля объекта напрямую без проверки
(<code>r.assignment || {}</code>, <code>a.manual === true</code> вместо просто
<code>a.manual</code>, <code>Array.isArray(a.explanation) ? a.explanation : []</code> и
так далее) — теперь даже совсем неожиданная форма ответа от сервера не уронит всю
отрисовку молча; (2) обернул вызов <code>renderMatching()</code> после обеих операций
(<code>loadMatchStatus</code> и клик по «Обновить предложения») в
<code>safeRenderMatching()</code> — если отрисовка всё-таки упадёт, теперь появится
явный красный баннер с ошибкой и текст в консоли, а не тихое "ничего не произошло"; (3)
добавил под списком карточек сворачиваемый блок «Отладка: сырой ответ сервера» —
показывает ровно тот JSON, который вернул <code>/api/match</code>, без какой-либо
обработки фронтендом, плюс <code>console.log</code> с тем же содержимым при каждой
загрузке. Это специально сделано так, чтобы можно было своими глазами свериться, что
конкретно прислал сервер (в частности, поле <code>manual</code> у каждого назначения),
не разбираясь во вкладке Network браузера.</p>
<p><strong>Честно:</strong> я по-прежнему не смог воспроизвести саму проблему —
выполнить фронтенд в браузере в этой сессии нельзя, поэтому не знаю точно, была ли
там реальная ошибка рендеринга или дело было в чём-то другом (например, в
пересборке Docker-образа backend'а, о которой писал раньше). Этот раунд правок делает
две вещи: закрывает конкретный класс возможных багов (тихий обрыв рендера) и — что
важнее прямо сейчас — даёт способ увидеть правду напрямую на экране через
отладочный JSON-блок, вместо того чтобы гадать дальше. Если после обновления
страницы (кэш статики — версия <code>?v=9</code>) в этом JSON-блоке видно
<code>"manual": true/false</code> с верными значениями, а карточки всё равно
выглядят одинаково — значит баг именно в <code>renderMatchCard()</code>, и это
нужно будет прислать мне скриншотом или текстом блока, чтобы я поправил точечно.</p>

<h4>24. Найдена настоящая причина «ничего не меняется» — ненадёжная запись файлов в папку на Рабочем столе (после этапа 23)</h4>
<p><strong>Разработчик:</strong> логи backend'а (добавленные на этапе 22) чётко показали,
что <code>run_matching()</code> реально отработал правильно — создал новые автоматические
назначения, ручные не тронул. Но на экране во вкладке «Подбор» всё равно ничего не менялось.</p>
<p><strong>AI:</strong> раз backend по логам работает верно, а на экране нет изменений —
остаётся только путь nginx → браузер. Я не мог заставить разработчика бесконечно
проверять руками одно и то же, поэтому вместо очередного вопроса скачал файл
<code>frontend/js/app.js</code> прямо с его компьютера через мост к устройству и сравнил
с тем, что должно быть там лежать. Оказалось: на диске действительно была версия БЕЗ
последних правок (без <code>renderMatchCard</code>), хотя мой инструмент записи файлов
в предыдущий раз отрапортовал об успехе. То есть сам факт "запись прошла успешно" не
гарантировал, что файл реально долетел и остался на диске — похоже, что-то на компьютере
разработчика (скорее всего синхронизация OneDrive: проект лежит в
<code>...\Desktop\ghost_relocation_service</code>, а Рабочий стол на Windows часто
синхронизируется с OneDrive по умолчанию) могло откатывать файл обратно сразу после
записи. Перезаписал файл ещё раз и тут же скачал обратно, чтобы сверить побайтово —
совпало. Тот же самый эффект повторился чуть позже с файлом
<code>matching_service.py</code> (мелкая правка формата лога) — первая попытка записи
тоже "успешно" завершилась, но при проверке скачиванием обратно файл оказался старым;
со второй попытки записалось и подтвердилось. Это, скорее всего, и есть настоящий корень
почти всех "я всё поправил, а ничего не изменилось" в этой сессии — правки могли реально
не долетать до диска раза через один, независимо от того, насколько правильным был сам
код. С этого момента после каждой important записи файла буду скачивать его обратно и
сверять, а не просто доверять отчёту "written" от инструмента.</p>
<p><strong>Честно:</strong> я не могу утверждать на 100%, что причина именно в OneDrive —
это наиболее вероятное объяснение, но подтвердить его я не могу (нет доступа к
файловой системе компьютера разработчика напрямую, кроме как через этот же ненадёжный
мост). Если проблема повторится снова, стоит либо на время выключить синхронизацию
OneDrive для этой папки, либо перенести проект в папку вне Рабочего стола (например,
<code>C:\dev\ghost_relocation_service</code>), где такой синхронизации обычно нет.</p>

<h3>Решения, принятые разработчиком самостоятельно</h3>
<ol>
  <li>Стек: FastAPI + PostgreSQL + Alembic + uv на бэкенде, обычный JS/CSS/HTML на фронтенде,
  упаковка в Docker Compose.</li>
  <li>Публичный деплой отложен — на этом этапе всё работает только локально.</li>
  <li>Push финального кода в GitHub-репозиторий разработчик делает сам, а не поручает AI.</li>
  <li>Состав особых условий привидений и типов мест переселения — задан в исходном ТЗ.</li>
  <li>Политика ручного выбора места: жёсткие конфликты (например, переполненность или явное
  нарушение условия) блокируются всегда без возможности "продавить"; мягкие проблемы (например,
  неидеальная температура) можно подтвердить и всё равно назначить — именно так было
  сформулировано в ТЗ ("если выбор плохой — предупредить, что не так").</li>
  <li>Архитектура бэкенда после первой версии переведена на явные три слоя —
  repository / service / endpoint — и модели оставлены только в виде SQLAlchemy-классов,
  без параллельного набора dataclass'ов.</li>
  <li>Файлы дальше разложены по core/db/schemas, а зависимости uv (pyproject.toml)
  перенесены из <code>backend/</code> в корень репозитория как общие для всего проекта.</li>
  <li><code>.env</code>/<code>.env.example</code> сделаны едиными на весь проект, вне
  <code>backend/</code> и <code>frontend/</code>.</li>
  <li>Команда запуска убрана из Dockerfile, миграции Alembic вынесены в отдельный
  сервис <code>migrate</code> в docker-compose.yml — так виден явный шаг, который
  их применяет, а не спрятанная в образе команда.</li>
  <li>Из кода убраны все построчные <code>#</code>/<code>//</code>-комментарии, но
  docstring'и осознанно оставлены как часть документации кода, а не "шум".</li>
  <li>Сидинг демо-данных и раздача фронтенда вынесены из backend'а в отдельные сервисы
  (<code>seed</code> и <code>nginx</code>) — backend занимается только API, а запуск
  всего проекта происходит исключительно через Docker Compose, без отдельного режима
  "без Docker" для самого приложения.</li>
  <li>Кнопка «Обновить предложения» реально назначает места (пакетно, с учётом
  конкуренции нескольких заявок за одно место) — с этим поведение прошло полный круг:
  сперва сохраняло автоматически, на этапе 18 стало только предлагать, на этапе 21
  снова стало сохранять, но теперь с явными тегами "назначено автоматически" /
  "автоматически не получилось" / "ручной выбор", чтобы не путать источники правды,
  как это было раньше. Оператор по-прежнему может в любой момент переназначить
  вручную.</li>
</ol>

<h3>Где AI мог ошибиться или предложить неидеальное решение</h3>
<ul>
  <li>Алгоритм подбора — <strong>жадный</strong> (greedy): заявки обрабатываются по одной в порядке
  срочности, а не оптимизируются глобально. Это значит, что в редких случаях общее число
  расселённых привидений может быть не максимально возможным (настоящая оптимизация потребовала бы
  венгерского алгоритма или похожей техники паросочетания).</li>
  <li>Пороги "мягкого" рейтинга (например, что рейтинг ниже 40/100 — это предупреждение) выбраны
  AI на глаз, без реальных данных бюро для калибровки — в реальном продукте их стоит проверить
  на исторических случаях.</li>
  <li>Как честно указано выше — интеграционные и юнит-тесты не были фактически запущены через
  pytest в этой сессии из-за сетевых ограничений песочницы; проверялась только логика алгоритма
  отдельным скриптом.</li>
  <li>Не проводилась проверка доступности (accessibility) фронтенда и кросс-браузерное тестирование.</li>
  <li>Рефакторинг на repository/service/endpoint (этап 7) тоже не прогонялся через pytest/uvicorn
  по той же причине — проверен только компиляцией и внимательным ручным чтением каждого метода.
  Логика подбора не менялась, но склейка слоёв (транзакции, порядок commit/flush) — новый код,
  и именно его в первую очередь стоит прогнать тестами перед сдачей.</li>
  <li>На этапе 12 (удаление комментариев) первая версия скрипта на <code>tokenize</code> содержала
  баг, который молча обрезал два файла миграций — конкретный пример того, как автоматическая
  правка кода без реального запуска тестов может незаметно сломать файл. Ошибка была замечена
  и исправлена в той же сессии (см. "Честно" в этапе 12), но это повод перепроверить именно
  Alembic-миграции особенно внимательно перед сдачей.</li>
</ul>

<h3>Что доработать вручную перед сдачей</h3>
<ul>
  <li>Реально запустить <code>docker compose up</code> и пройти чек-лист сценариев ниже.</li>
  <li>Выполнить <code>pytest</code> внутри контейнера/venv и убедиться, что все тесты зелёные.</li>
  <li>Проверить UI глазами: формы, вкладки, подсказки об ошибках.</li>
  <li>Решить вопрос публичного хостинга (Render/Railway/другое), если он нужен для итоговой сдачи.</li>
</ul>

<h3>Что улучшить, если бы это был реальный продукт</h3>
<ul>
  <li>Заменить жадный алгоритм на честную оптимизацию паросочетания (например, венгерский алгоритм),
  чтобы максимизировать число расселённых привидений, а не просто удовлетворить самых срочных.</li>
  <li>Аутентификация и роли операторов, журнал аудита (кто и когда менял назначение).</li>
  <li>Импорт заявок и мест из CSV/Excel, экспорт отчёта.</li>
  <li>Уведомления о приближающемся дедлайне переселения.</li>
  <li>CI/CD с автотестами на каждый PR и реальный деплой с managed Postgres.</li>
  <li>Более гибкая модель "особых условий" — конструктор правил вместо фиксированного enum.</li>
  <li>End-to-end тесты (например, Playwright) поверх текущих unit/integration тестов.</li>
  <li>Если фронтенд начнёт расти — перейти на React/Vue вместо чистого JS.</li>
</ul>
`;
